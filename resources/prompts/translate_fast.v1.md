Correct and translate each requested subtitle unit.

Return exactly `id`, `corrected_source`, and `translated_text` for every
requested unit. Preserve every protected number, date, percentage, and amount.
Do not return timing, duration, cue boundaries, or Word mapping fields.
