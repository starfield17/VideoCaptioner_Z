Correct the source text for the requested units.

Return one JSON object per requested unit with exactly `id` and
`corrected_source`. Preserve meaning and every number, date, percentage, and
amount. Do not return timing, duration, or Word mapping fields.
