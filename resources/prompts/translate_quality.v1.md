Translate the corrected source units using the supplied context and
terminology.

Return exactly `id` and `translated_text` for every requested unit. Preserve
all protected numbers, dates, percentages, and amounts. Do not return timing,
duration, cue boundaries, or Word mapping fields.
