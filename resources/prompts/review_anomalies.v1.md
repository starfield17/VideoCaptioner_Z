Review only the requested anomalous subtitle units.

Return exactly `id` and `translated_text` for every requested anomaly. Keep
the intended meaning, terminology, protected numbers, dates, percentages, and
amounts. Do not return timing, duration, cue boundaries, or Word mapping.
