Extract terminology for the requested source units.

Return exactly `id`, `source_term`, and `target_term` for every requested unit.
Use the requested unit ID unchanged. Do not return timestamps, durations, cue
boundaries, Word mapping, or any other fields.
